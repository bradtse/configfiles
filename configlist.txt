# This is the config list that the python script parses. When adding a new
# config to this folder, make sure to add a new entry to this file. Make sure
# to follow the format of the other entries.
# format: config_name:install_path
ackrc:~/.ackrc
bashrc:~/.bashrc
conkyrc:~/.conkyrc
dircolorsrc:~/.dircolors
gitconfig:~/.gitconfig
terminatorconfig:~/.config/terminator/config
vimrc:~/.vimrc
zshrc:~/.zshrc
